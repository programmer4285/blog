---
import Posts from "../../components/Posts.astro";

import { getCollection } from "astro:content";

const posts = (await getCollection("posts")).filter((post) => !post.data.draft);
---

<Posts title="Posts" heading="Posts" posts={posts} />
